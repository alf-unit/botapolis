/* /ru/login — Russian mirror of the login page.
   The shared login page reads its locale from x-locale and ships RU
   strings for Magic Link / Google CTAs. */
export { default, generateMetadata } from "@/app/(auth)/login/page"
