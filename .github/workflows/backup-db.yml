# Weekly Supabase database backup → GitHub artifact.
# Runs every Monday 04:00 UTC + can be triggered manually.
# Restore: download the .sql.gz artifact, gunzip, `psql $DB_URL < file.sql`.
#
# Required GitHub Secret:
#   SUPABASE_DB_URL — Supabase Session-mode pooler connection string.
#   Get it in: Supabase Dashboard → Project Settings → Database →
#              Connection string → "Session pooler" tab → URI.
#              Format: postgresql://postgres.[ref]:[pw]@aws-0-[region].pooler.supabase.com:5432/postgres
#   IMPORTANT: must be SESSION mode (port 5432), not Transaction (port 6543).
#   pg_dump needs session-level statements that Transaction mode rejects.

name: Database backup

on:
  schedule:
    # Mondays at 04:00 UTC (07:00 Moscow). Cron is UTC.
    - cron: "0 4 * * 1"
  workflow_dispatch:
    # Manual trigger: Actions tab → "Database backup" → "Run workflow".

permissions:
  contents: read

jobs:
  backup:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    # PG client major version. Must be >= the Supabase server's PG major.
    # Supabase = 17 as of May 2026. When they upgrade, bump this single value
    # and both steps below pick it up via /usr/lib/postgresql/${PG_VERSION}/bin.
    env:
      PG_VERSION: "17"

    steps:
      - name: Install PostgreSQL ${{ env.PG_VERSION }} client
        # ubuntu-latest (24.04) ships with postgresql-client-16 preinstalled.
        # Installing postgresql-client-17 alongside works, BUT a bare `pg_dump`
        # call goes through pg_wrapper (postgresql-client-common), which often
        # still resolves to the older binary. That's exactly why the previous
        # run failed with "server version: 17.6; pg_dump version: 16.13".
        # The dump step therefore uses the absolute path; this step just
        # installs and sanity-checks that the right binary is reachable.
        run: |
          set -euo pipefail
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc \
            -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] \
            https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update -qq
          sudo apt-get install -y -qq "postgresql-client-${PG_VERSION}"
          PG_DUMP="/usr/lib/postgresql/${PG_VERSION}/bin/pg_dump"
          test -x "$PG_DUMP" || { echo "::error::pg_dump not found at $PG_DUMP"; exit 1; }
          "$PG_DUMP" --version
          "$PG_DUMP" --version | grep -q " ${PG_VERSION}\." \
            || { echo "::error::pg_dump version mismatch (expected major ${PG_VERSION})"; exit 1; }

      - name: Dump database
        env:
          # Never expose this in logs. `set +x` is the default; no echo of $DB_URL anywhere.
          DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          set -euo pipefail
          if [ -z "${DB_URL:-}" ]; then
            echo "::error::SUPABASE_DB_URL secret is empty or unset."
            exit 1
          fi
          DATE=$(date -u +%Y-%m-%d)
          mkdir -p backups
          OUT="backups/botapolis-${DATE}.sql"
          # Absolute path bypasses pg_wrapper — see install-step comment for why.
          PG_DUMP="/usr/lib/postgresql/${PG_VERSION}/bin/pg_dump"
          # --clean + --if-exists → drop-then-create on restore (idempotent).
          # --no-owner / --no-privileges → portable across Supabase projects.
          # --quote-all-identifiers → defensive against reserved-word collisions.
          # --exclude-schema=auth → Supabase Auth schema is managed by Supabase;
          #   restoring it can collide with the live auth system. We back up
          #   our public schema; Supabase manages auth.users separately.
          "$PG_DUMP" "$DB_URL" \
            --clean --if-exists \
            --no-owner --no-privileges \
            --quote-all-identifiers \
            --exclude-schema=auth \
            --exclude-schema=storage \
            --exclude-schema=realtime \
            --exclude-schema=supabase_functions \
            --exclude-schema=vault \
            > "$OUT"
          gzip -9 "$OUT"
          ls -lh backups/
          # Sanity: gzipped dump should be > 1KB. Empty/near-empty = something went wrong.
          SIZE=$(stat -c%s "${OUT}.gz")
          if [ "$SIZE" -lt 1024 ]; then
            echo "::error::Dump suspiciously small ($SIZE bytes). Check connection or schema."
            exit 1
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: db-backup-${{ github.run_id }}
          path: backups/*.sql.gz
          retention-days: 90
          if-no-files-found: error
          compression-level: 0 # already gzipped
